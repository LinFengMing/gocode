package main

import (
	"fmt"
)

func main() {
	var a int
	var b float32
	var c float64
	var isMarried bool
	var name string
	// %v 按照變數的值輸出
	fmt.Printf("a = %d, b = %v, c = %v, isMarried = %v, name = %v", a, b, c, isMarried, name)
}
